<% if task.errors.present? %>
  $("#modal-form-body").html("<%= j render partial: 'form', locals: { task: task } %>")
<% else %>
  $("#task-modal").modal('hide')
  window.location.href = '/tasks'
<% end %>